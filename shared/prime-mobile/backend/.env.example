# PRIME Mobile API - Environment Variables
# Copy to .env and fill in real values

# Server
PORT=8080
NODE_ENV=production

# Google OAuth (for mobile app login)
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# Google Drive access (service account or refresh token)
GOOGLE_REFRESH_TOKEN=your-refresh-token
GOOGLE_DRIVE_FOLDER_ID=prime-folder-id-on-drive

# Claude API
CLAUDE_API_KEY=sk-ant-your-key-here

# Gemini API
GEMINI_API_KEY=your-gemini-key-here

# JWT signing secret (generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_SECRET=your-jwt-secret-here

# Allowed user email (single-user security)
ALLOWED_EMAIL=stephen@bender23.com
